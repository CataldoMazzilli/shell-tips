cd
	vai alla home dell'utente che esegue il comando

~/.bashrc
	percorso file di configurazione (utente) di bash

~/.bash_aliases
	percorso file alias (utente)

git add -A
	aggiunge i file pronti per il commit

git commit -m "messaggi di testo"
	fa il commit sul repository locale

git push origin master
	carica tutti i commit sul repository remoto

git pull origin master 
	scarica gli aggiornamenti dal repository remoto

cat nomefile
	stampa nel terminale il contenuto ascii del file (nomefile può essere un percorso relativo o assoluto)
	
